[project]
name = "support-knowledge-copilot"
version = "1.0.0"
description = "Production RAG support assistant with hybrid retrieval and verified citations"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
